/* Write a Program that defines a function to
   calculate the sum of two integers and prints the result.
   Call this function by passing two integer values. */


/* Approach -
    1. Define a function that takes two numbers as a input.
    2. Add the two numbers inside the function.
    3. Call the function with two numbers And Print the result. */

function sum(a,b){
    let add = a + b;
    console.log(add);  
}

sum(7,7);